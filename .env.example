# Contact form delivery (optional).
# Without these the API validates the enquiry and logs it server-side, but does
# not email anyone. Set all three to deliver enquiries by email via Resend.
RESEND_API_KEY=
CONTACT_FROM_EMAIL="Blue Point Website <website@yourdomain.com>"
CONTACT_TO_EMAIL=temidayo@bluepoint.ng
